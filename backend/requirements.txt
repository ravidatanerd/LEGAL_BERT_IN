# InLegalDesk Backend - Maximum Compatibility Requirements
# Uses highest available versions that work with our code structure

# Core FastAPI stack (widely available versions)
fastapi>=0.65.0
uvicorn[standard]>=0.13.0
python-multipart>=0.0.5
pydantic>=1.8.0

# AI and ML libraries (maximum available versions)
transformers>=4.5.0,<=4.18.0
torch>=1.7.0
sentence-transformers>=2.0.0
numpy>=1.19.0

# Document processing (maximum available)
PyMuPDF>=1.16.0,<=1.19.6
Pillow>=8.0.0
pytesseract>=0.3.7
opencv-python>=4.2.0

# Text processing
rank-bm25>=0.2.0
markdown>=3.2.0

# HTTP and async
httpx>=0.20.0
aiofiles>=0.6.0
requests>=2.25.0

# Environment and config
python-dotenv>=0.15.0

# Security (basic compatible versions)
cryptography>=3.0.0

# Optional dependencies (install separately if needed)
# faiss-cpu>=1.6.0  # May not be available on all systems
# protobuf>=3.15.0  # Install manually if hybrid models needed
# sentencepiece>=0.1.85  # Install manually for T5/XLNet