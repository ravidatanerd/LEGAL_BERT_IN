# InLegalDesk Backend - Pre-compiled Wheels for Windows Python 3.6+
# Avoids compilation issues by using specific versions with reliable wheels

# Essential: Install wheel support first
wheel>=0.36.0
setuptools>=40.0.0

# Core FastAPI stack (Python 3.6+ compatible)
fastapi>=0.65.0,<0.75.0
uvicorn[standard]>=0.13.0,<0.16.0
python-multipart>=0.0.5,<0.0.6
pydantic>=1.8.0,<1.9.0

# Essential math library (Python 3.6 compatible)
numpy>=1.19.0,<1.20.0

# AI libraries (specific versions with wheels for Python 3.6)
# Note: torch CPU version will be installed separately via script
torch>=1.7.0,<1.11.0
transformers==4.12.5
sentence-transformers==2.1.0

# Document processing (specific wheel versions)
PyMuPDF==1.19.6
Pillow>=8.0.0,<9.0.0
pytesseract>=0.3.7,<0.4.0

# Computer vision (headless version - no GUI dependencies, more reliable wheels)
opencv-python-headless==4.5.5.64

# Text processing
rank-bm25>=0.2.0
markdown>=3.2.0

# HTTP and async
httpx>=0.20.0
aiofiles>=0.6.0
requests>=2.25.0

# Environment and config
python-dotenv>=0.15.0

# Security (basic compatible versions)
cryptography>=3.0.0

# Optional dependencies (install separately if needed)
# faiss-cpu>=1.6.0  # May not be available on all systems
# protobuf>=3.15.0  # Install manually if hybrid models needed
# sentencepiece>=0.1.85  # Install manually for T5/XLNet